/**\n * UI FUNCTIONS MODULE\n * Phase 5: Advanced ES Modules\n */\n\nexport function renderQuestion(question, index) {\n  const html = `\n    <div class=\"question\">\n      <h3>${question.text}</h3>\n      ${question.gambar ? `<img src=\"${question.gambar}\" alt=\"Question\">` : ''}\n      <div class=\"options\">\n        ${question.opsi.map((opt, i) => `\n          <label>\n            <input type=\"radio\" name=\"answer\" value=\"${i}\">\n            ${opt.text}\n            ${opt.gambar ? `<img src=\"${opt.gambar}\" alt=\"Option\">` : ''}\n          </label>\n        `).join('')}\n      </div>\n    </div>\n  `;\n  return html;\n}\n\nexport function renderResults(results) {\n  const html = `\n    <div class=\"results\">\n      <h2>Hasil Ujian</h2>\n      <p>Skor: ${results.score}/${results.total}</p>\n      <p>Persentase: ${((results.score/results.total)*100).toFixed(2)}%</p>\n    </div>\n  `;\n  return html;\n}\n\nexport function updateUI(selector, html) {\n  const element = document.querySelector(selector);\n  if (element) {\n    element.innerHTML = html;\n  }\n}\n