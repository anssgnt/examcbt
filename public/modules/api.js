/**\n * API CALLS MODULE\n * Phase 5: Advanced ES Modules\n */\n\nexport async function getExamData(examId) {\n  try {\n    const result = await gasRun('getExamData', examId);\n    return result;\n  } catch (err) {\n    console.error('Error fetching exam data:', err);\n    throw err;\n  }\n}\n\nexport async function submitAnswer(examId, questionIndex, answer) {\n  try {\n    const result = await gasRun('submitAnswer', {\n      examId,\n      questionIndex,\n      answer,\n      timestamp: Date.now()\n    });\n    return result;\n  } catch (err) {\n    console.error('Error submitting answer:', err);\n    throw err;\n  }\n}\n\nexport async function submitExam(examId, answers) {\n  try {\n    const result = await gasRun('submitExam', {\n      examId,\n      answers,\n      timestamp: Date.now()\n    });\n    return result;\n  } catch (err) {\n    console.error('Error submitting exam:', err);\n    throw err;\n  }\n}\n\nexport async function getResults(examId) {\n  try {\n    const result = await gasRun('getResults', examId);\n    return result;\n  } catch (err) {\n    console.error('Error fetching results:', err);\n    throw err;\n  }\n}\n